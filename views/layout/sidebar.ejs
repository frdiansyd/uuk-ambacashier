<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sidebar</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;700&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Plus Jakarta Sans', sans-serif;
        }

        body {
            background-color: #f5f5f5;
        }

        aside a,
        aside a:hover,
        aside a span {
            text-decoration: none !important;
        }

        aside {
            width: 250px;
            height: 100vh;
            background: #1A120B;
            color: white;
            padding: 25px 15px;
            display: flex;
            flex-direction: column;
            position: fixed;
            left: 0;
            top: 0;
        }


        aside nav ul {
            padding: 0;
            margin: 50px 0px;
            list-style: none;
        }

        aside nav li {
            margin-bottom: 0.75rem;
        }

        aside nav a {
            width: 100%;
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            background-color: #1A120B;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            font-weight: 500;
            transition: background 0.3s ease;
        }


        aside nav a span {
            flex: 1;
        }

        aside nav a:hover {
            background-color: #2a1e15;
        }


        .logout-form {
            margin-top: auto;
        }

        .logout-form button {
            width: 100%;
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 0.75rem 1rem;
            background-color: #1A120B;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            font-weight: 500;
            transition: background 0.3s ease;
        }

        .logout-form button:hover {
            background-color: #2a1e15;
        }
    </style>
</head>

<body>
    <aside class="sidebar">
        <img src="/assets/main_logo.png" width="165px" alt="AmbaCashier Logo" />
        <nav>
            <% if (user && (user.role==="petugas" || user.role==="admin" )) { %>
                <ul>
                    <li><a href="/produk"></i><span>Daftar Produk</span></a></li>
                    <li><a href="/penjualan"></i><span>Laporan Penjualan</span></a></li>
                    <li><a href="/produk/tambah"></i><span>Tambah Produk</span></a></li>
                </ul>
                <% } else if (user && user.role==='pelanggan' ) { %>
                    <ul>
                        <li><a href="/produk/pelanggan"></i><span>Daftar Produk</span></a></li>
                        <li><a href="/keranjang"></i><span>Keranjang</span></a></li>
                    </ul>
                    <% } %>
        </nav>

        <% if (user) { %>
            <form action="/auth/logout" method="GET" class="logout-form">
                <button type="submit">
                    Logout
                </button>
            </form>
            <% } %>
    </aside>
</body>


</html>